networks:
  default:
    external: {name: media-microservices-overlay-2}
services:
  movie-review-mongodb:
    hostname: movie-review-mongodb
    image: mongo
    ports: ['27017:27022']
    restart: always
version: '3'
